#!/usr/bin/env php
<?php
namespace BrainCalc\Cli;
/**
 * PHP version 2222
 * fgh@category $this is a category.
 * ghj@package $this is a category.
 * fty@author $this is a category.
 * lkj@license $this is a category.
 * nbv@link $this is a category.
 */

$autoloadPath1 = __DIR__ . '/../../../autoload.php';
$autoloadPath2 = __DIR__ . '/../vendor/autoload.php';

if (file_exists($autoloadPath1)) {
    require_once $autoloadPath1;
} else {
    require_once $autoloadPath2;
}

use function cli\line;
use function cli\prompt;
use function cli\out;
use function cli\err;

line("Welcome to the Brain Game!");
$name = prompt("May I have your name?");
line("Hello, %s!", $name);
line("Answer \"yes\" if given number is prime. Otherwise answer \"no\".");
$count = 0;
$correctAnswerCount = 3;

function primeCheck($number){
    if ($number === 1)
    return 0;
    for ($i = 2; $i <= $number/2; $i++){
        if ($number % $i == 0)
            return 0;
    }
    return 1;
}

while ($count < $correctAnswerCount) {
    $primeNumber = rand(1, 100);

    line("Question: " . $primeNumber);
    $answer = prompt("Your answer");
    if (primeCheck($primeNumber) === 1) {
        if ($answer == "yes") {
            out("Correct! \n");
            $count++;
        } else {
            err($answer . " is wrong answer ;(. Correct answer was \"yes\". \n
Let\'s try again," . $name . "!");
            $count = 0;
        }
    } elseif (primeCheck($primeNumber) === 0) {
        if ($answer == "no") {
            out("Correct! \n");
            $count++;
        } else {
            err($answer . " is wrong answer ;(. Correct answer was \"no\". \n
Let\'s try again, " . $name . "!");
            $count = 0;
        }
    }
}
if ($count == 3) {
    out("Congratulations, " . $name . "!");
}
